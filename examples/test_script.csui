# Test COMSUI script
block --info "g_check"

if g_check; then
    info "In a git repository"
    block --gitop "g_status"
else
    warn "Not in a git repository"
fi

r_upgrade "admin" sh -c 'echo $(whoami)'